version: "3"
services:
  ecomposteira-notification:
    container_name: ecomposteira-notification
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    volumes:
      - .:/app
      - /app/node_modules
